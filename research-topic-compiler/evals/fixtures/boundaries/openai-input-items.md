# OpenAI Responses Input Items - Frozen Eval Excerpt

- Fixture ID: `openai-input-items`
- Canonical identity: `https://platform.openai.com/docs/api-reference/responses/create`
- Evidence type: official API documentation

Frozen summary: a Responses API input can be a text string or a list of typed input items. Items can represent messages or other supported structured inputs, preserving role and content structure instead of flattening everything into one string.

Scope limitation: this excerpt explains the concept, not every currently supported item type.
