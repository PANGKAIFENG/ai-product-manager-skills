# Agent Skills Specification - Frozen Eval Excerpt

- Fixture ID: `agent-skills-spec`
- Canonical identity: `https://agentskills.io/specification`
- Evidence type: public specification
- Lineage root: `agent-skills-specification`

Frozen summary: a Skill directory requires `SKILL.md` with identifying metadata and instructions. Optional directories can hold reusable scripts, references, and assets. The entry description communicates what the Skill does and when it applies; bundled resources remain optional and are loaded or executed only when needed.

This fixture directly challenges the seed's single-file-only claim. It defines package compatibility, not whether every optional resource improves quality.
