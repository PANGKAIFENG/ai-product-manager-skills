# MCP SDK Transport Examples - Frozen Eval Excerpt

- Fixture ID: `mcp-sdk-implementation`
- Evidence role: Canonical implementation

Frozen observations: SDK examples configure local stdio and remote HTTP transports differently. Local examples inherit a controlled process environment; remote examples expose explicit authentication and network boundaries.

This shows implementation shape but is not an independent security audit.
