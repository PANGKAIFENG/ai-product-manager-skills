# Fixture Seed: 所有 MCP 服务都必须接 OAuth

> Frozen fictional secondary material for transfer evaluation.

- Fixture ID: `mcp-auth-seed-blog`
- Source type: secondary engineering blog
- Evidence role: discovery only

文章声称：“无论本地 stdio 还是远程 HTTP，只要是 MCP Server，就必须部署同一套 OAuth 流程；否则一定不安全。”文章没有引用协议规范，也没有区分进程边界、传输方式、凭据来源或威胁模型。
