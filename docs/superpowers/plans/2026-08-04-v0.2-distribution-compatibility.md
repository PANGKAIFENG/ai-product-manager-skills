# v0.2 Distribution Compatibility Implementation Plan

> 执行分支：`v0.2.0-restructure`
>
> 工作树：`/Users/linctex/Documents/Codex/ai-product-manager-skills-v0.2`
>
> 边界：只处理 v0.2 目录迁移后的安装与分发兼容性；不合并 `main`、不创建或合并 PR、不覆盖本地已修改 Skill、不运行全局 `skillshare sync`。

**目标：** 让 `skills/<skill-id>/` 成为清晰、可验证的唯一安装源，同时说明旧版根目录安装与 v0.2 的兼容关系，并验证 GitHub 分支能发现全部 13 个 Skills。

**方法：** 仓库继续保持稳定 `skill-id`，只改变源仓库中的物理子目录。公开安装文档说明全仓库发现、单 Skill 安装和旧元数据迁移；维护者文档增加 fail-closed 检查，先 dry-run，再决定是否覆盖本地内容。

## 执行清单

- [x] 1. 固化基线与实施计划
  - 检查分支、工作树和提交边界。
  - 确认原始脏工作区不在本次命令的工作目录内。
  - 保存本计划，并在每个阶段完成后更新复选框。

- [x] 2. 补充 v0.2 迁移说明
  - 新增 `docs/migration-v0.2.md`。
  - 说明根目录 `<skill-id>/` 到 `skills/<skill-id>/` 的路径映射。
  - 说明稳定 `skill-id`、触发名称和 Skill 内容不因本次移动而改变。
  - 分开说明现有本地副本、符号链接和 Skillshare source metadata 的影响。

- [x] 3. 更新安装与维护者文档
  - 更新 `docs/README.md`，加入迁移指南入口。
  - 更新 `docs/install-codex.md` 和 `docs/install-claude-code.md`，加入 v0.1 -> v0.2 升级说明和单 Skill 新路径。
  - 更新 `docs/local-distribution.md`，加入元数据检查、dry-run 和禁止广泛同步的保护步骤。
  - 更新 `CHANGELOG.md`，记录迁移与兼容文档。

- [x] 4. 本地验证文档和仓库质量门禁
  - 仓库结构审计：

    ```bash
    python3 scripts/audit_skills.py .
    ```

  - PRD 回归测试：

    ```bash
    python3 -m unittest discover -s skills/prd-architect/tests -p 'test_*.py'
    ```

  - Research Dashboard 回归测试：

    ```bash
    python3 -m unittest discover -s skills/research-topic-compiler/tests -p 'test_*.py'
    ```

  - 文档与 diff 检查：

    ```bash
    git diff --check
    python3 scripts/audit_skills.py .
    ```

- [ ] 5. 提交并只推送 v0.2 分支
  - 再次确认当前分支为 `v0.2.0-restructure`。
  - 提交迁移兼容文档。
  - 只执行：

    ```bash
    git push -u origin v0.2.0-restructure
    ```

- [ ] 6. 从 GitHub 分支验证 Skillshare 发现结果
  - 运行只读 dry-run：

    ```bash
    skillshare install PANGKAIFENG/ai-product-manager-skills \
      -b v0.2.0-restructure \
      --dry-run \
      --all
    ```

  - 确认输出发现且选择全部 13 个稳定 Skill ID。
  - 不执行真实安装，不执行 `skillshare sync`。

- [ ] 7. 评估本机 `prd-architect` 元数据迁移
  - 只读检查本地 `.metadata.json` 中的 `source`、`repo_url`、`subdir` 和 `version`。
  - 对新路径运行定向 dry-run：

    ```bash
    skillshare install \
      PANGKAIFENG/ai-product-manager-skills/skills/prd-architect \
      -b v0.2.0-restructure \
      --name prd-architect \
      --dry-run
    ```

  - 比较本地内容和分支版本，若存在本地修改则停止，不执行 `--force`。
  - 输出后续安全迁移命令及其前置条件，不在本阶段覆盖本地 Skill。

## 完成条件

- 迁移与安装文档清楚回答“目录移动是否影响本地使用”。
- 所有本地质量门禁通过，共 13 个 Skills、41 个回归测试。
- 远端仅新增 `v0.2.0-restructure` 分支。
- GitHub 分支 dry-run 发现全部 13 个 Skills。
- 本机现有 `prd-architect` 内容保持不变，并给出经过 dry-run 验证的后续迁移路径。
