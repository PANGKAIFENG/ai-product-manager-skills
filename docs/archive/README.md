# Archive

This directory preserves completed planning issues, historical audits, and release-promotion material.

Archived documents are snapshots of the repository state when they were written. Paths and commands inside them may describe the pre-v0.2 root-level Skill layout and are not current installation guidance. Use the repository `README.md`, `SKILL_REGISTRY.md`, `SKILL_ROUTING.md`, and `catalog/skills.yaml` for the current structure.
